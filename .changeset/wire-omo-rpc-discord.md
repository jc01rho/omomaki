---
'kimaki': minor
---

Route Discord thread prompts through classic `omo --mode rpc --session` instead of OpenCode `promptAsync`. Existing vitest suites keep OpenCode unless `KIMAKI_USE_OMO_RPC=1`.
