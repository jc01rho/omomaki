---
'kimaki': minor
---

Add the omomaki omo-bridge foundation: SQLite tables for thread bindings, outbound message identity, and security audit, plus a fail-closed approval bridge and text-only input translation toward replacing OpenCode with omo RPC.
